services:
  pocketbase:
    image: ghcr.io/muchobien/pocketbase:latest
    container_name: inspection-pb
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - pb_data:/pb/pb_data
      - ./pocketbase/pb_schema.json:/pb/pb_schema.json:ro

volumes:
  pb_data:
